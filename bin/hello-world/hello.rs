// Main function which acts as the entry point to execute code
fn main() {
  /* 
   * { println } is a normal function
   * But if we use { println! } with (!), it becomes a "macro"
   * We pass a string { "Hello World!" }
   */
  println!("Hello World!");
}